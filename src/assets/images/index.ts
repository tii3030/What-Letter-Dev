const images = {
    back: require('./back.png'),
    configurations: require('./configurations.png'),
    checkAll: require('./select(1).png'),
    uncheckAll: require('./unchecked(1).png'),
}
  
export default images